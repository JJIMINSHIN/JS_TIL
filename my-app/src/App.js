import './App.css';
import Template from './component/Template';
import TodoList from './component/TodoList';

const App = () =>{
  return (
    <Template>
      <TodoList/>
    </Template>
  );
}

export default App;
